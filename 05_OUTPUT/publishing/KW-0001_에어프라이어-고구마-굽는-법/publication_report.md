# Publication Report — KW-0001

- Keyword ID: KW-0001
- Title: 에어프라이어 고구마 굽는 법: 크기별 온도와 시간, 안 익을 때 해결법
- Mode: **EXPORT_ONLY**
- Quality: 92.95 (A-), Critical 0 / Major 0 / Minor 2
- WordPress: 비활성 — API 호출 없음

## 생성 파일

`content_final.md`, `content_final.html`, `content_wordpress.html`, `publication_payload.json`, `wordpress_payload.json`(NOT_SUBMITTED), `schema.json`, `metadata.json`, `sources.json`, `internal_links.json`(empty), `media_manifest.json`(PENDING_ASSET), `publication_checklist.md`, 이 파일.

## 남은 작업 (수동)

- 대표 이미지 준비 후 `media_manifest.json` 갱신
- 관련 콘텐츠가 추가되면 `internal_links.json` 갱신
- WordPress 게시가 필요해지면 별도로 `wordpress_config.yaml`/`publication_config.yaml`의 `wordpress.enabled`를 명시적으로 `true`로 바꾸고 WF-07을 `WORDPRESS_DRAFT` 모드로 재실행 (자동 공개는 이 저장소 정책상 여전히 비활성)
